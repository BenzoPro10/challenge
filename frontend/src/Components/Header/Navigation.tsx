import React from "react";
import { Box, Button } from "@mui/material";

const Navigation: React.FC = () => (
  <Box>
    <Button color="inherit">Discover Ello</Button>
    <Button color="inherit">Parent Resources</Button>
  </Box>
);

export default Navigation;
